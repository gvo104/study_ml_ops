FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn prometheus-client

COPY drift ./drift

ENV PYTHONPATH=/app

CMD ["uvicorn", "drift.monitoring.app:app", "--host", "0.0.0.0", "--port", "9108"]
